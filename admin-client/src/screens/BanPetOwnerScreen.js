import React from "react";
import { View, StyleSheet, Text } from "react-native";

const BanPetOwnerScreen = ({ navigation }) => {
    return (
        <>
            <Text>Ban BanPetOwnerScreen</Text>
        </>
    );
};

const styles = StyleSheet.create({});

export default BanPetOwnerScreen;
