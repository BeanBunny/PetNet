import React from "react";
import { View, StyleSheet } from "react-native";
import { Headline } from "react-native-paper";

const HomeScreen = () => {
    return <Headline>HomeScreen</Headline>;
};

const styles = StyleSheet.create({});

export default HomeScreen;
