import React from "react";
import { View, StyleSheet } from "react-native";
import { Headline } from "react-native-paper";

const AccountScreen = () => {
    return <Headline>AccountScreen</Headline>;
};

const styles = StyleSheet.create({});

export default AccountScreen;
